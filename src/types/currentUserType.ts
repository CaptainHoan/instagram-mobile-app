export type AUTH_USER_PROFILE = {
    email: string,
    id: string,
    timestamp: [],
    username: string,
    profilePicture: string
}

export type POST_TYPE = {
    post_id: string,
    username: string,
    status: string,
    image: string,
    profilePicture: string,
    timestamp: Date,
}